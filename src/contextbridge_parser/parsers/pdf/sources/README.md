# Source-Specific PDF Parsers

This directory contains parsers for specific PDF sources whose structure or
business schema needs custom extraction logic.

Reusable PDF utilities belong in `../common/`.

Reusable parser profiles belong in `../profiles/`.
